{
  "name": "socket.io_cluster_example",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod:start": "pm2 start ./app.js -i 0 && pm2 flush && pm2 logs",
    "prod:stop": "pm2 delete app"
  },
  "keywords": [
    "cluster",
    "express",
    "socket.io"
  ],
  "author": "Joseph Chang",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.2",
    "pm2": "^2.10.1",
    "socket.io": "^2.0.4"
  }
}
